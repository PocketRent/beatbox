<?php

// Copy this to conf/env.php, change the values to suit
define('DEV_MODE', true);

// This is passed as the application_name to Postgres and
// the prefix for Redis
// define('APP_NAME', 'name')
